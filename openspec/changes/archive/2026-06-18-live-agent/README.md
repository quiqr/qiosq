# live-agent

E8 — run a configured real coding agent (Claude over rmux) live in the interactive TUI: start a detached session, send the open file's intent, stream the agent pane each tick. Fake agent stays the CI path.
