# agent-bridge

E6 — read-only file viewer + the send-to-agent bridge: an Agent trait (Claude Code over rmux-sdk), @path intent injection, completion-sentinel detection, and a fake agent for deterministic tests.
