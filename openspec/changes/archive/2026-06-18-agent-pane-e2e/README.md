# agent-pane-e2e

E7 — render the agent pane (ratatui-rmux) and prove the whole flow in a NixOS VM booting Quiqr Server: list site, serve via hugo, open file read-only, Ask AI injects intent, fake agent writes content, assert on disk.
